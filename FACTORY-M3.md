# SparkOS 内容工厂 M3：周三 / 周六编辑策划

M3 在每日情报排名之上新增一个确定性编辑层。模型负责把情报簇补充为结构化事实、证据与独立判断；程序负责跨日聚合、选题排序、持久化和人工闸门。

## 两个编辑窗口

- `midweek`：截至指定日期向前 4 天，适合周三复盘最近几天并捕捉正在加速的话题。
- `weekly`：截至指定日期向前 7 天，适合周六识别连续霸榜和已经演化为结构性议题的话题。
- 每次最多输出 5 张卡。只有最新证据等级为 A/B 的话题可以入选；不足 5 个时不以 C/D 级话题补位。

## 选题卡

每张 `editorial_card` 固化以下字段：

- 趋势模式：连续霸榜、加速上升、二次升温、事实反转、结构议题或新兴话题；
- 核心判断与“为什么现在”；
- 已确认事实、逐条证据、知识卡回链；
- 反方意见、风险、平台与内容形式；
- A/B 证据等级、预期价值分和人工决策状态。

卡片写入 SQLite `editorial_runs` / `editorial_cards`，同时在通用 `approvals` 表创建 `pending` 审批。选题卡逐张批准或驳回；存在未处理卡片时工作流保持 `waiting_approval`，全部处理后才进入 `succeeded`。

## 运行

```text
sparkos_run {action:"topics", payload:{editorial:"midweek"}}
sparkos_run {action:"topics", payload:{editorial:"weekly"}}
```

可传 `payload.date="YYYY-MM-DD"` 回放指定截止日。工作台“创作中心 → 选题推荐”会展示最新策划并提供批准/驳回按钮。

设置 `SPARKOS_EDITORIAL_SCHEDULE=1` 后，系统按机器本地时区在周三、周六 20:00 尝试生成对应策划。同一模式、日期与排名输入使用幂等任务键；调度只生成待审卡，不生成草稿、不发布。

## M3 边界

- 不自动补造证据，也不把未核验传言包装成选题；
- 不自动批准选题；
- M3 本身不生成草稿；已批准选题由 M4 自动创建结构化创作任务；
- 不自动发布（发布仍属于后续人工闸门）。
